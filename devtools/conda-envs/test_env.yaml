
name: test
channels:
  - conda-forge
  - psi4
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - psi4
  - matplotlib

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

